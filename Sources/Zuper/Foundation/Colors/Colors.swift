import SwiftUI


public extension Color {

    // MARK: - Blue
    /// Zuper Blue Dark color.
    static var blueDark = Color("Blue Dark", bundle: .current)
    /// Zuper Blue Dark Active color.
    static var blueDarkActive = Color("Blue Dark Active", bundle: .current)
    /// Zuper Blue Dark Hover color.
    static var blueDarkHover = Color("Blue Dark Hover", bundle: .current)
    /// Zuper Blue Light color.
    static var blueLight = Color("Blue Light", bundle: .current)
    /// Zuper Blue Light Active color.
    static var blueLightActive = Color("Blue Light Active", bundle: .current)
    /// Zuper Blue Light Hover color.
    static var blueLightHover = Color("Blue Light Hover", bundle: .current)
    /// Zuper Blue Normal color.
    static var blueNormal = Color("Blue Normal", bundle: .current)
    /// Zuper Blue Normal Active color.
    static var blueNormalActive = Color("Blue Normal Active", bundle: .current)
    /// Zuper Blue Normal Hover color.
    static var blueNormalHover = Color("Blue Normal Hover", bundle: .current)

    // MARK: - Cloud
    /// Zuper Cloud Dark color.
    static var cloudDark = Color("Cloud Dark", bundle: .current)
    /// Zuper Cloud Dark Active color.
    static var cloudDarkActive = Color("Cloud Dark Active", bundle: .current)
    /// Zuper Cloud Dark Hover color.
    static var cloudDarkHover = Color("Cloud Dark Hover", bundle: .current)
    /// Zuper Cloud Light color.
    static var cloudLight = Color("Cloud Light", bundle: .current)
    /// Zuper Cloud Light Active color.
    static var cloudLightActive = Color("Cloud Light Active", bundle: .current)
    /// Zuper Cloud Light Hover color.
    static var cloudLightHover = Color("Cloud Light Hover", bundle: .current)
    /// Zuper Cloud Normal color.
    static var cloudNormal = Color("Cloud Normal", bundle: .current)
    /// Zuper Cloud Normal Active color.
    static var cloudNormalActive = Color("Cloud Normal Active", bundle: .current)
    /// Zuper Cloud Normal Hover color.
    static var cloudNormalHover = Color("Cloud Normal Hover", bundle: .current)

    // MARK: - Green
    /// Zuper Green Dark color.
    static var greenDark = Color("Green Dark", bundle: .current)
    /// Zuper Green Dark Active color.
    static var greenDarkActive = Color("Green Dark Active", bundle: .current)
    /// Zuper Green Dark Hover color.
    static var greenDarkHover = Color("Green Dark Hover", bundle: .current)
    /// Zuper Green Light color.
    static var greenLight = Color("Green Light", bundle: .current)
    /// Zuper Green Light Active color.
    static var greenLightActive = Color("Green Light Active", bundle: .current)
    /// Zuper Green Light Hover color.
    static var greenLightHover = Color("Green Light Hover", bundle: .current)
    /// Zuper Green Normal color.
    static var greenNormal = Color("Green Normal", bundle: .current)
    /// Zuper Green Normal Active color.
    static var greenNormalActive = Color("Green Normal Active", bundle: .current)
    /// Zuper Green Normal Hover color.
    static var greenNormalHover = Color("Green Normal Hover", bundle: .current)

    // MARK: - Ink
    /// Zuper Ink Dark color.
    static var inkDark = Color("Ink Dark", bundle: .current)
    /// Zuper Ink Dark Active color.
//    static var inkDarkActive = Color("Ink Dark Active", bundle: .current) // Not used
    /// Zuper Ink Dark Hover color.
//    static var inkDarkHover = Color("Ink Dark Hover", bundle: .current) // Not Used
    /// Zuper Ink Light color.
    static var inkLight = Color("Ink Light", bundle: .current)
    /// Zuper Ink Light Active color.
//    static var inkLightActive = Color("Ink Light Active", bundle: .current) // not used
    /// Zuper Ink Light Hover color.
//    static var inkLightHover = Color("Ink Light Hover", bundle: .current) // not used
    /// Zuper Ink Normal color.
    static var inkNormal = Color("Ink Normal", bundle: .current)
    /// Zuper Ink Normal Active color.
 //   static var inkNormalActive = Color("Ink Normal Active", bundle: .current) // not used
    /// Zuper Ink Normal Hover color.
    static var inkNormalHover = Color("Ink Normal Hover", bundle: .current)

    // MARK: - Orange
    /// Zuper Orange Dark color.
    static var orangeDark = Color("Orange Dark", bundle: .current)
    /// Zuper Orange Dark Active color.
    static var orangeDarkActive = Color("Orange Dark Active", bundle: .current)
    /// Zuper Orange Dark Hover color.
    static var orangeDarkHover = Color("Orange Dark Hover", bundle: .current)
    /// Zuper Orange Light color.
    static var orangeLight = Color("Orange Light", bundle: .current)
    /// Zuper Orange Light Active color.
    static var orangeLightActive = Color("Orange Light Active", bundle: .current)
    /// Zuper Orange Light Hover color.
    static var orangeLightHover = Color("Orange Light Hover", bundle: .current)
    /// Zuper Orange Normal color.
    static var orangeNormal = Color("Orange Normal", bundle: .current)
    /// Zuper Orange Normal Active color.
    static var orangeNormalActive = Color("Orange Normal Active", bundle: .current)
    /// Zuper Orange Normal Hover color.
    static var orangeNormalHover = Color("Orange Normal Hover", bundle: .current)

    // MARK: - Product
    /// Zuper Product Dark color.
    static var productDark = Color("Product Dark", bundle: .current)
    /// Zuper Product Dark Active color.
    static var productDarkActive = Color("Product Dark Active", bundle: .current)
    /// Zuper Product Dark Hover color.
    static var productDarkHover = Color("Product Dark Hover", bundle: .current)
    /// Zuper Product Darker color.
    static var productDarker = Color("Product Darker", bundle: .current)
    /// Zuper Product Light color.
    static var productLight = Color("Product Light", bundle: .current)
    /// Zuper Product Light Active color.
    static var productLightActive = Color("Product Light Active", bundle: .current)
    /// Zuper Product Light Hover color.
    static var productLightHover = Color("Product Light Hover", bundle: .current)
    /// Zuper Product Normal color.
    static var productNormal = Color("Product Normal", bundle: .current)
    /// Zuper Product Normal Active color.
    static var productNormalActive = Color("Product Normal Active", bundle: .current)
    /// Zuper Product Normal Hover color.
    static var productNormalHover = Color("Product Normal Hover", bundle: .current)

    // MARK: - Red
    /// Zuper Red Dark color.
    static var redDark = Color("Red Dark", bundle: .current)
    /// Zuper Red Dark Active color.
    static var redDarkActive = Color("Red Dark Active", bundle: .current)
    /// Zuper Red Dark Hover color.
    static var redDarkHover = Color("Red Dark Hover", bundle: .current)
    /// Zuper Red Light color.
    static var redLight = Color("Red Light", bundle: .current)
    /// Zuper Red Light Active color.
    static var redLightActive = Color("Red Light Active", bundle: .current)
    /// Zuper Red Light Hover color.
    static var redLightHover = Color("Red Light Hover", bundle: .current)
    /// Zuper Red Normal color.
    static var redNormal = Color("Red Normal", bundle: .current)
    /// Zuper Red Normal Active color.
    static var redNormalActive = Color("Red Normal Active", bundle: .current)
    /// Zuper Red Normal Hover color.
    static var redNormalHover = Color("Red Normal Hover", bundle: .current)
    
    // MARK: - Indico
    /// Zuper Indico Dark color.
    static var indicoDark = Color("Indico Dark Hover", bundle: .current)
    /// Zuper Indico Dark Hover color.
    static var indicoDarkHover = Color("Indico Dark", bundle: .current)
    /// Zuper Indico Light color.
    static var indicoLight = Color("Indico Light", bundle: .current)
    /// Zuper Indico Light Active color.
    static var indicoLightActive = Color("Indico Light Active", bundle: .current)
    /// Zuper Indico Light Hover color.
    static var indicoLightHover = Color("Indico Light Hoverr", bundle: .current)
    /// Zuper Indico Normal color.
    static var indicoNormal = Color("Indico Normal", bundle: .current)
    /// Zuper Indico Normal Hover color.
    static var indicoNormalHover = Color("Indico Normal Hover", bundle: .current)

    // MARK: - White
    /// Zuper White color.
    static var whiteNormal = Color("White", bundle: .current)
    /// Zuper White Darker color.
    static var whiteDarker = Color("White Darker", bundle: .current)
    /// Zuper White Lighter color.
    static var whiteLighter = Color("White Lighter", bundle: .current)
    
    // MARK: - Icon
    /// Zuper Icon Primary color.
    static var iconPrimary = Color("Icon Primary", bundle: .current)
    /// Zuper Icon Primary color.
    static var iconSecondary = Color("Icon Secondary", bundle: .current)

    // MARK: - Apple HIG Semantic Aliases

    // MARK: Text Colors (similar to Apple's label hierarchy)

    /// Primary text color - Use for main content and titles.
    /// Maps to Apple HIG's `.label` semantic color.
    static var textPrimary: Color { .inkDark }

    /// Secondary text color - Use for subtitles and supporting text.
    /// Maps to Apple HIG's `.secondaryLabel` semantic color.
    static var textSecondary: Color { .inkNormal }

    /// Tertiary text color - Use for placeholder text and disabled states.
    /// Maps to Apple HIG's `.tertiaryLabel` semantic color.
    static var textTertiary: Color { .inkLight }

    /// Inverse text color - Use for text on dark backgrounds.
    static var textInverse: Color { .whiteNormal }

    // MARK: Background Colors (similar to Apple's systemBackground hierarchy)

    /// Primary background color - Main screen background.
    /// Maps to Apple HIG's `.systemBackground` semantic color.
    static var backgroundPrimary: Color { .whiteNormal }

    /// Secondary background color - Cards, grouped content.
    /// Maps to Apple HIG's `.secondarySystemBackground` semantic color.
    static var backgroundSecondary: Color { .cloudLight }

    /// Tertiary background color - Nested groups, inset content.
    /// Maps to Apple HIG's `.tertiarySystemBackground` semantic color.
    static var backgroundTertiary: Color { .cloudNormal }

    // MARK: Separator Colors

    /// Standard separator color for dividers and borders.
    /// Maps to Apple HIG's `.separator` semantic color.
    static var separator: Color { .cloudNormal }

    /// Opaque separator color for non-transparent dividers.
    /// Maps to Apple HIG's `.opaqueSeparator` semantic color.
    static var separatorOpaque: Color { .cloudDark }

    // MARK: Fill Colors (for UI elements)

    /// Primary fill color for filled UI elements.
    static var fillPrimary: Color { .cloudLight }

    /// Secondary fill color for subtle fills.
    static var fillSecondary: Color { .cloudNormal }

    // MARK: Status Colors (semantic meaning)

    /// Success color - Use for success states, confirmations.
    /// Maps to Apple HIG's `.systemGreen`.
    static var statusSuccess: Color { .greenNormal }

    /// Success background - Light background for success states.
    static var statusSuccessBackground: Color { .greenLight }

    /// Warning color - Use for warnings, caution states.
    /// Maps to Apple HIG's `.systemOrange`.
    static var statusWarning: Color { .orangeNormal }

    /// Warning background - Light background for warning states.
    static var statusWarningBackground: Color { .orangeLight }

    /// Error/Destructive color - Use for errors, destructive actions.
    /// Maps to Apple HIG's `.systemRed`.
    static var statusError: Color { .redNormal }

    /// Error background - Light background for error states.
    static var statusErrorBackground: Color { .redLight }

    /// Info color - Use for informational states.
    /// Maps to Apple HIG's `.systemBlue`.
    static var statusInfo: Color { .blueNormal }

    /// Info background - Light background for info states.
    static var statusInfoBackground: Color { .blueLight }

    // MARK: Interactive Colors

    /// Primary tint/accent color - Brand color for interactive elements.
    /// Maps to Apple HIG's `.tint` semantic color.
    static var tint: Color { .productNormal }

    /// Link color - Use for tappable text links.
    static var link: Color { .blueNormal }

    /// Disabled color - Use for disabled/inactive elements.
    static var disabled: Color { .cloudDark }
}
